<odoo>
    <data>
        <record id="view_company_goal_form" model="ir.ui.view">
            <field name="name">company.goal.form</field>
            <field name="model">company.goal</field>
            <field name="arch" type="xml">
                <form string="Company Goal">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="description"/>
                            <field name="deadline"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Add more views for company goals if needed -->
        <!-- ... -->
    </data>
</odoo>
